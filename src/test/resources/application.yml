spring:
  application:
    name: parcel-delivery
  threads:
    virtual:
      enabled: true
  profiles:
    active: test

springdoc:
  swagger-ui:
    path: /api-ui

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
